import { test, expect } from '@playwright/test';

test('Deve carregar a pagina principal da Quitanda S.A.', async ({ page }) => {
  // Teste basico de garantia do front-end
  expect(true).toBe(true);
});